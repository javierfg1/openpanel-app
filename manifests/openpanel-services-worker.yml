apiVersion: v1
kind: Service
metadata:
  name: openpanel-worker-active
  namespace: openpanel
spec:
  selector:
    app: openpanel-worker
  ports:
    - port: 8089
      targetPort: 9090

---

apiVersion: v1
kind: Service
metadata:
  name: openpanel-worker-preview
  namespace: openpanel
spec:
  selector:
    app: openpanel-worker
  ports:
    - port: 8089
      targetPort: 9090