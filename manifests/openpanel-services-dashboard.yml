apiVersion: v1
kind: Service
metadata:
  name: openpanel-dashboard-active
  namespace: openpanel
spec:
  selector:
    app: openpanel-dashboard
  ports:
    - port: 8088
      targetPort: 3000

---

apiVersion: v1
kind: Service
metadata:
  name: openpanel-dashboard-preview
  namespace: openpanel
spec:
  selector:
    app: openpanel-dashboard
  ports:
    - port: 8088
      targetPort: 3000